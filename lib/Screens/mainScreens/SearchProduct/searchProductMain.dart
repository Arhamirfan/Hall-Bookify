import 'package:flutter/material.dart';

class SearchProduct extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.lightBlueAccent,
        child: Center(
          child: Text('This is 2nd screen search product'),
        ),
      ),
    );
  }
}
